import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const primaryColor = Color(0xFF628C1B);
  static const secondaryColor = Color(0xFFFAD27A);
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
  static const cancelledColor = Color(0xffDB4437);
  /// Snackbar background when offline (YouTube-style grey).
  static const offlineSnackbarColor = Color(0xFF424242);
  /// Snackbar background when back online (green).
  static const onlineSnackbarColor = Color(0xFF2E7D32);
}
